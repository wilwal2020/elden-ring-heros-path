# Your own route history. It is your play, not the program, and it is large:
# routes.db alone is 8 MB after a month.
data/

# The stitched source images. The tile pyramids built from them are checked in
# -- see docs/NOTICE.md -- but these are 33 MB each and only needed to cut the
# tiles again, which nobody has to do.
m1.png
m1-underground.png

# Python leavings
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor and OS leavings
.vscode/
.idea/
Thumbs.db
desktop.ini
*.swp
Untitled*
